class Background {
    constructor(spacing, lineColour) {
        this.spacing = spacing;
        this.lineColour = lineColour;
    }

    Render(p5, viewZoom, viewPosition) {
        let amountX = this.spacing;

    }
}

export default Background;